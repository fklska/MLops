// FastAPI
uvicorn app.main:app --reload

pip install psycopg[binary] -i https://mirrors.aliyun.com/pypi/simple/

// Postgres
docker run --name PGTEST -p 5433:5432 -e POSTGRES_PASSWORD=1234567 -e POSTGRES_USER=postgres -e POSTGRES_DB=app -d postgres


// Alembic
alembic revision --autogenerate -m "Add label&status review"
alembic upgrade head

// Git
git add .
git commit -m "message"
git push
git pull
git fetch
git push -u origin <branch_name>
git checkout -b <branch_name>


// Worker
docker run --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:4-management
celery -A main worker