apiVersion: v1
kind: Service
metadata:
  name: evidently-service
  namespace: kinootziv-app
spec:
  selector:
    app: evidently
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
  type: ClusterIP