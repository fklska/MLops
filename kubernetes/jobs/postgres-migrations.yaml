apiVersion: batch/v1
kind: Job
metadata:
  name: postgres-migration
  namespace: kinootziv-app
  annotations:
    argocd.argoproj.io/sync-options: Skip=true,Prune=false
spec:
  template:
    spec:
      containers:
      - name: migrations
        image: fklska/kino-backend:1.70
        imagePullPolicy: Always
        command: 
          - "sh"
          - "-c"
          - "alembic upgrade head"
      
        envFrom:
        - configMapRef:
            name: backend-cm 
      restartPolicy: Never
