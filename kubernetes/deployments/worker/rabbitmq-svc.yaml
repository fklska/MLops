apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-svc
  namespace: kinootziv-app
spec:
  selector:
    app: rabbitmq
  ports:
  - port: 5672
    targetPort: 5672
    name: "api"
  - port: 15672
    targetPort: 15672
    name: "web"
