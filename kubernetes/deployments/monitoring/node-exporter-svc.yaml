apiVersion: v1
kind: Service
metadata:
  name: node-exporter-svc
  namespace: kinootziv-app
spec:
  selector:
    app: node-exporter
  clusterIP: None 
  ports:
  - port: 9100
    protocol: TCP
    targetPort: 9100
    name: metrics
