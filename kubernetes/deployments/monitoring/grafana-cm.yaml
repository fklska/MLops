apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-cm
  namespace: kinootziv-app
data:
  datasource.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: http://prometheus-svc:9090
        isDefault: true
